# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Metro
.metro-health-check*

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# generated native folders
/ios
/android

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files
.env*
!.env.example

# typescript
*.tsbuildinfo

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Supabase CLI local cache
supabase/.temp/

# AI Stuff (instructions, skills, agents, etc.)
.superpowers/
# Process artifacts from the superpowers workflow (spec/plan) — useful during
# this development phase, not living reference for a clone or collaborator.
# docs/supabase-setup.md stays tracked; it's the one doc a fresh clone needs.
docs/superpowers/
## Impeccable — per-machine consent/config only; PRODUCT.md/DESIGN.md are tracked
.impeccable/config.local.json
# Impeccable design-detector hook runtime cache (per-session state, not skill source)
.claude/skills/impeccable/scripts/.impeccable/
